name: Docker Workflow Unit 6

on:
  push:
    branches:
      - unit-6

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t my-docker-app-unit6 .
